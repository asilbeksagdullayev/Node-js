const os = require('os');

// Platform
console.log(os.platform());

// Information
console.log(os.cpus());

// Architecture
console.log(os.arch());

// Free memory
console.log(os.freemem());

//Total memory
console.log(os.totalmem());

// Enter point
console.log(os.homedir());
